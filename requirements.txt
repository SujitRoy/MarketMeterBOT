# MarketMeter Dependencies
# Install: pip install -r requirements.txt
#
# NSE BhavCopy is fetched directly from the official archive CSV, so nsefin is
# no longer required. See src/data/fetchers/nse_bhavcopy.py for the rationale
# (nsefin dropped the AVG_PRICE column and masked 404s as empty DataFrames).
#
# Versions are pinned to the majors this code was verified against. pandas 3.x
# and python-telegram-bot 22.x are untested here; an unpinned install resolved
# # to both and is a silent breakage risk.

# Core Data
pandas>=2.2,<3.0
numpy>=1.26,<3.0

# Telegram Bot
# The [job-queue] extra pulls APScheduler. Without it Application.job_queue is
# None and every scheduled job (18:30 sync, 08:30 report) silently never runs.
python-telegram-bot[job-queue]>=21.0,<22.0

# HTTP
requests>=2.31.0

# Fuzzy Search
rapidfuzz>=3.0,<4.0

# Template Engine
jinja2>=3.1,<4.0

# Environment
python-dotenv>=1.0,<2.0

# Timezone
pytz>=2024.1,<2025.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0

# Code Quality
black>=24.0,<25.0
mypy>=1.9,<2.0
ruff>=0.4,<1.0

# Optional: FastBT for backtesting
# fastbt>=0.1,<1.0